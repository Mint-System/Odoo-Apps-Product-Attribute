<?xml version="1.0" encoding="utf-8" ?>
<odoo>
  <record id="product_pricelist_item_form_view" model="ir.ui.view">
    <field
      name="name"
    >product_pricelist_fixed_discount.product_pricelist_item_form_view</field>
    <field name="model">product.pricelist.item</field>
    <field name="inherit_id" ref="product.product_pricelist_item_form_view" />
    <field name="arch" type="xml">
      <label for="percent_price" position="replace">
        <label
          for="percent_price"
          string="Discount"
          invisible="compute_price != 'fixed'"
        />
        <div class="o_row gap-3" invisible="compute_price != 'fixed'">
          <field name="percent_price" />
          <span>%</span>
        </div>
      </label>
    </field>
  </record>

  <record id="product_pricelist_view" model="ir.ui.view">
    <field name="name">product_pricelist_fixed_discount.product_pricelist_view</field>
    <field name="model">product.pricelist</field>
    <field name="inherit_id" ref="product.product_pricelist_view" />
    <field name="arch" type="xml">
      <field name="date_start" position="before">
        <field name="percent_price" />
      </field>

      <xpath expr="//list[1]/field[1]" position="before">
        <field name="sequence" widget="handle" />
      </xpath>
    </field>
  </record>
</odoo>
